<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:minHeight="56dp"
    android:orientation="horizontal"
    android:paddingStart="16dp"
    android:paddingTop="10dp"
    android:paddingEnd="16dp"
    android:paddingBottom="10dp">

    <ImageView
        android:id="@+id/paymentRowIcon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:importantForAccessibility="no"
        android:scaleType="centerInside"
        tools:src="@drawable/ic_cash_payment" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/paymentRowTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/uklon_on_surface"
            android:textSize="16sp"
            tools:text="Готівка" />

        <TextView
            android:id="@+id/paymentRowSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/uklon_on_surface_secondary"
            android:textSize="13sp"
            android:visibility="gone"
            tools:text=""
            tools:visibility="visible" />
    </LinearLayout>

    <ImageView
        android:id="@+id/paymentRowCheck"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_check"
        android:visibility="gone"
        app:tint="@color/uklon_on_surface"
        tools:visibility="visible" />

    <ImageView
        android:id="@+id/paymentRowDelete"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/un_link_token"
        android:padding="8dp"
        android:scaleType="centerInside"
        android:src="@drawable/ic_baseline_delete_24"
        android:visibility="gone"
        app:tint="@color/uklon_on_surface_secondary"
        tools:visibility="visible" />
</LinearLayout>
