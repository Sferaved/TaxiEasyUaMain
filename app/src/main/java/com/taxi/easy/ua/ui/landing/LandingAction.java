package com.taxi.easy.ua.ui.landing;

/**
 * Действия с гостевой статической страницы (после входа — переход в приложение).
 */
public enum LandingAction {
    ORDER,
    LANGUAGE,
    CALCULATION,
    CITY,
    PAYMENT,
    OPERATOR,
    DRIVER
}
